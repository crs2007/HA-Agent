# Secrets
.env
services/ha-task-server/.env   # SQL login password for the task-board container
*.token
secrets.yaml

# HA raw inventory snapshots (point-in-time exports — may contain tokens, PII, device IPs)
_bmad/bmm/knowledge/inventory/raw/

# Log files (contain internal IPs, node/container names)
*.log

# Playwright MCP session artifacts (HA URLs, auth errors)
.playwright-mcp/

# Task board — track tasks.json, exclude backup snapshots
_bmad/bmm/tasks/backups/

# Local Claude settings with tokens
.claude/settings.json
.claude/settings.local.json
.claude/ha-mcp-audit.log
.mcp.json

# Node / Playwright
node_modules/
playwright-report/
blob-report/
test-results/
.auth/
playwright/.auth/
.playwright/
*.png
*.zip

# Home Assistant database and runtime
home-assistant_v2.db
home-assistant_v2.db-shm
home-assistant_v2.db-wal

# HA Storage (UI-managed, large JSON)
.storage/

# HA runtime directories
tts/
.cloud/
backups/
__pycache__/

# OS
Thumbs.db
.DS_Store
desktop.ini

# IDE
.vscode/
.idea/
tools/_scratch/

# Claude
CLAUDE.md
.claude/
