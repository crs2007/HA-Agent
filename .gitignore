# Secrets
.env
*.token
secrets.yaml

# HA raw inventory snapshots (point-in-time exports — may contain tokens, PII, device IPs)
_bmad/bmm/knowledge/inventory/raw/

# Log files (contain internal IPs, node/container names)
*.log

# Playwright MCP session artifacts (HA URLs, auth errors)
.playwright-mcp/

# Generated task artifacts (device IPs, entity IDs, PII)
_bmad/bmm/tasks/

# Local Claude settings with tokens
.claude/settings.json
.claude/settings.local.json
.claude/ha-mcp-audit.log
.mcp.json

# Node
node_modules/

# Home Assistant database and runtime
home-assistant_v2.db
home-assistant_v2.db-shm
home-assistant_v2.db-wal

# HA Storage (UI-managed, large JSON)
.storage/

# HA runtime directories
tts/
.cloud/
backups/
__pycache__/

# OS
Thumbs.db
.DS_Store
desktop.ini

# IDE
.vscode/
.idea/
tools/_scratch/
